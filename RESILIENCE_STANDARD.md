核心原则：优雅降级，苹果级负面反馈。

1. 错误捕获规范
静默重试：网络波动引发的 API 失败，必须在后台自动重试 3 次，且不阻塞 UI。

原子化操作：如果保存资产失败，系统必须通过 rollback 回退到修改前的状态，防止出现数据碎片。

2. 交互反馈 (Feedback)
触感反馈：出错时，输入框或卡片执行轻微的 Shake (震动) 动画。

直观提示：报错信息禁止直接显示原始 JSON（如 42P02），必须转换为：“创意大脑暂时离线，正在尝试重新连接...”。
