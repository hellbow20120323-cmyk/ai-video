核心原则：先诊断后编码，小步快跑。

1. 开发指令模板
指令要求：每次开发新功能前，Cursor 必须先执行 “架构自检”。

自检项：

“检查此变更是否会导致 page.tsx 超过 250 行？”

“此逻辑是否破坏了三阶段流转的确定性？”

“Device ID 的绑定是否仍然有效？”

2. 提交与日志记录
详细日志：要求 Cursor 在 Console 中打印关键节点，格式统一为：[Workflow: ${State}] -> ${Action} -> ${Status}，方便在 macOS 的开发者工具中快速排查。
